<project>

    <!-- Steam -->
    
    <macrodef name = "steam-build">
        
        <element name = "arguments" optional = "true" />
        
        <sequential>
            <exec executable = "steam">
                <arg value = "build"/>
                <arguments/>
            </exec>
        </sequential>
    
    </macrodef>

</project>